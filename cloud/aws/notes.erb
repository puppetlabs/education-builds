<%= @event[:notes] %>

Your <%= @event[:course_name] %> classroom has been started.

You can access the classroom master using:

  * Address: <%= @event[:address] %>
  * Dashboard: <%= @event[:dashboard] %>
  * Zoom URL: <%= @event[:session_url] %>

<% if @event[:instance_data] -%>
To log in to Windows instances, browse to https://thinrdp.classroom.puppet.com/

Please distribute the Windows instance IP address and Administrator password
to each student as needed. Enter them along with the Administrator username
into the ThinRDP UI to log in to the student Windows instance.

<% @event[:instance_data].each do |instance, password_data| -%>
<% unless password_data['password'].nil? -%>
<%= password_data['name'] %>: <%= password_data['private_ip'] %> / <%= password_data['password'] %>
<% end -%>
<% end -%>
<% end -%>

This machine will be shut down on <%= @event[:termination_date] %>.
If you promise more time to students, please inform eduteam@puppet.com.
